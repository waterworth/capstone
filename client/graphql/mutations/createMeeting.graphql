mutation CreateMeeting(
  $title: String!
  $description: String!
  $timeslot: DateTime!
  $length: Int!
) {
  createMeeting(
    title: $title
    description: $description
    timeslot: $timeslot
    length: $length
  ) {
    ...RegularMeeting
  }
}
