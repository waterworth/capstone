mutation UpdateMeeting(
  $id: Int!
  $title: String!
  $timeslot: DateTime!
  $length: Int!
  $description: String!
) {
  updateMeeting(
    id: $id
    title: $title
    timeslot: $timeslot
    length: $length
    description: $description
  ) {
    ...RegularMeeting
  }
}
